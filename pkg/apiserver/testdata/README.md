`sa-test.key` / `sa-test.pub` — an RSA keypair for unit tests that sign and
verify ServiceAccount tokens (RS256). Generated for the tests and used nowhere
else; it is not a secret and signs nothing real.
